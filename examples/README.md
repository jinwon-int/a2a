# examples

- `local/` - loopback echo quickstart fixture.
- `workers/standalone-http-worker/` - review-only standalone HTTP worker card.
- `workers/hermes-reference-worker/` - Hermes-style broker-agnostic worker dry-run script and loopback smoke fixture.
